[alias]
    log1 = "!f() { git log --oneline | head -n ${1-10}; }; f"
    cm = commit -m
    br = checkout -b
    append = commit --amend --no-edit
    scs = "!f() { git stash && git checkout \"$1\" && git stash pop }; f"
 
[pager]
    log = false
    branch = false

